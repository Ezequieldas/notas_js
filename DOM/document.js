/*

Algunos métodos para acceder desde el objeto document

*/

console.log(document); // Devuelve todo el documanto HTML
console.log(document.head); // Devuelve solo el head del documento HTML
console.log(document.body); // Devuelve solo el body del documento HTML
console.log(document.documentElement); // Devuelve todo el documento HTML
console.log(document.doctype); // Devuelve el doctype del documento HTML
console.log(document.charset); // Devuelve el charset (tipos de letras) del head
console.log(document.title); // Devuelve el title del head
console.log(document.images); // Devuelve las imagenes del body
console.log(document.forms); // Devuelve los formularios
console.log(document.styleSheets); // Devuelve las hojas de estilo relacionadas a ese doc
console.log(document.scripts); // Devuelve las hojas de estilo relacionadas a ese doc